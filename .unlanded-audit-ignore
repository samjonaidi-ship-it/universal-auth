# Merged PRs that audit-unlanded-work.mjs would otherwise flag as orphaned.
# Format: <pr-number> # <why it is resolved>
# Only add a line here once you have VERIFIED the content actually reached main.

2 # P4.6 DPoP proofs. Part of the seven-PR stacked chain p31 -> p46 -> p41 -> p411 -> p48 -> p5b -> p5c. On 2026-08-11 these were merged bottom-up with --delete-branch=false, which GitHub reports as "MERGED" while actually merging each PR into its PARENT BRANCH, not main; only #1 reached main. Caught by `git merge-base --is-ancestor`, never by the gh CLI output. RESOLVED the same day by a single tip->main PR (#8, head agent/p5c-drafts-flow), which carries every commit in the chain. Each of these branches now sits exactly ONE commit ahead of main -- its own mis-targeted merge commit -- so its ref will never be an ancestor. VERIFIED 2026-08-11 by content diff, not by ref topology: `git diff origin/main origin/agent/<branch> -- src/ package.json` is deletions-only plus the branch's older SDK_VERSION/watermark strings, i.e. main is a strict superset. Every public export was checked by name against origin/main:src/index.ts (listDevices, revokeDevice, AuthDevice, reportDeviceSyncState, getDraft, putDraft, deleteDraft, setPin, verifyPin, clearPin -- all present; the chain's single-line devices-flow export simply became a multi-line block). main is rc.19, and CalExp5 pins the rc.19 tarball whose contents were extracted and confirmed to hold all four features.
3 # P4.1 PIN flow through the SDK client. Same stacked-chain artifact as #2 -- see that entry for the full verification.
4 # P4.11 listDevices / revokeDevice. Same stacked-chain artifact as #2 -- see that entry for the full verification.
5 # rc.17 errorFromEnvelope surfaces the server's message. Same stacked-chain artifact as #2. Note main carries the LATER form of this line (`env.message ?? env.error ?? ...`); the branch holds the superseded rc.16 form, which is why the diff shows it as branch-only.
6 # rc.18 reportDeviceSyncState. Same stacked-chain artifact as #2 -- see that entry for the full verification.
7 # rc.19 getDraft / putDraft / deleteDraft. Same stacked-chain artifact as #2. Its head branch (agent/p5c-drafts-flow) IS an ancestor of main via #8; only the #7 merge commit is not.
